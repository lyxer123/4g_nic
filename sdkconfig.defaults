#
# Serial flasher config
#
CONFIG_ESPTOOLPY_FLASHSIZE_4MB=y

#
# Partition Table
#
CONFIG_PARTITION_TABLE_TWO_OTA=y

# Temporary Fix for Timer Overflows
CONFIG_FREERTOS_TIMER_TASK_STACK_DEPTH=4096

#
# ESP32-specific
#
CONFIG_MEMMAP_SMP=y
CONFIG_ESP_SYSTEM_EVENT_TASK_STACK_SIZE=4096

CONFIG_FREERTOS_UNICORE=n
CONFIG_FREERTOS_HZ=1000

CONFIG_ESP_INT_WDT=n
CONFIG_ESP_TASK_WDT_INIT=n

# LWIP
CONFIG_LWIP_L2_TO_L3_COPY=y
CONFIG_LWIP_IP_FORWARD=y
CONFIG_LWIP_IPV4_NAPT=y
CONFIG_LWIP_TCP_MSS=1460
CONFIG_LWIP_TCP_OVERSIZE_MSS=y
# SPI Ethernet + DHCP: ARP-check-on-offer can stall; optional client-id can upset some routers.
# CONFIG_LWIP_DHCP_DOES_ARP_CHECK is not set
CONFIG_LWIP_DHCP_DISABLE_CLIENT_ID=y

# Override some defaults to enable PPP
CONFIG_LWIP_PPP_SUPPORT=y
CONFIG_LWIP_PPP_PAP_SUPPORT=y
CONFIG_LWIP_TCPIP_TASK_STACK_SIZE=4096
# Do not enable IPV6 in dte<->dce link local
CONFIG_LWIP_PPP_ENABLE_IPV6=n
# Disable legacy API
CONFIG_MODEM_LEGACY_API=n

CONFIG_ETH_DMA_BUFFER_SIZE=1600

CONFIG_ETH_SPI_ETHERNET_DM9051=y
CONFIG_ETH_SPI_ETHERNET_KSZ8851SNL=y
CONFIG_ETH_SPI_ETHERNET_W5500=y

CONFIG_ESP_SYSTEM_MEMPROT_FEATURE=n

CONFIG_ESP_MAIN_TASK_STACK_SIZE=5120

# ESP-Iot-Bridge — W5500 as sole wired uplink (DHCP client), SoftAP + NAPT for Wi-Fi clients.
# Do not use AUTO_WAN_OR_LAN + ETH_LAN DHCPS on the same RJ45 as the router (breaks WAN DHCP).
CONFIG_BRIDGE_EXTERNAL_NETIF_STATION=y
CONFIG_BRIDGE_EXTERNAL_NETIF_ETHERNET=y
# CONFIG_BRIDGE_EXTERNAL_NETIF_MODEM is not set
CONFIG_BRIDGE_DATA_FORWARDING_NETIF_SOFTAP=y
# CONFIG_BRIDGE_DATA_FORWARDING_NETIF_ETHERNET is not set
# CONFIG_BRIDGE_NETIF_ETHERNET_AUTO_WAN_OR_LAN is not set
# W5500 emac addr: match board (Arduino ETH.begin PHY addr often 1)
CONFIG_BRIDGE_ETH_SPI_PHY_ADDR0=1
CONFIG_BRIDGE_ETH_SPI_CLOCK_MHZ=10

# Debug: wired WAN as 192.168.0.105/24 (change or disable via menuconfig → System options)
CONFIG_SYSTEM_ETH_WAN_USE_STATIC_IP=y
CONFIG_SYSTEM_ETH_WAN_STATIC_IP_STR="192.168.0.105"
CONFIG_SYSTEM_ETH_WAN_STATIC_GW_STR="192.168.0.1"
CONFIG_SYSTEM_ETH_WAN_STATIC_NM_STR="255.255.255.0"
